/**
 * Adds `.is-visible` to any element with `.reveal` once it scrolls into view.
 * Mounted once at the app root.
 */
export function useReveal() {
  // Reveal components now handle their own mount animations
}
